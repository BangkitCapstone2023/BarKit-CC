// const admin = require('firebase-admin');
// const initializeApp = require('../config/configFirebase');

// let db; // Variabel untuk menyimpan instance Firestore

// // Fungsi untuk mendapatkan instance Firestore
// async function getFirestoreInstance() {
//   if (!db) {
//     const firebaseApp = await initializeApp(); // Memanggil initializeApp() untuk mendapatkan instance aplikasi Firebase
//     db = firebaseApp.firestore(); // Mendapatkan instance Firestore dari aplikasi Firebase
//   }
//   return db;
// }

// module.exports = getFirestoreInstance;
